import React from 'react';
import { render } from 'react-dom';
import './header.scss'
function Header () {
    return (
        <header>
            <h1 className="header">RouteME</h1>
        </header>
    )
}

export default Header